# CTI Adversary Profile Report

The canonical, audited report source is `distillation_attacks_adversary_profile.tex`; the compiled submission is `distillation_attacks_adversary_profile.pdf`.

This Markdown file intentionally does not duplicate the full report. The earlier draft contained unsupported ATT&CK mappings and claims that could drift independently from the LaTeX source.

Supporting audit artifacts:

- `evidence_register.csv` - claim-level source, access date, confidence, and limitation
- `distillation_attack_navigator.json` - constrained ATT&CK analogies with confidence notes
- `generate_report_artifacts.py` - reproducible chart generation
- `data_transfer_package.md` - build and interpretation instructions
