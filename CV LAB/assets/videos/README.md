# Drop a traffic / dashcam video here as `traffic_sample.mp4` (Program 6).

The script will abort with a clear `FileNotFoundError` if this file is
absent and no internet is available to auto-fetch a public-domain clip.
