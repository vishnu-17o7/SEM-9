# Drop an ordered image sequence here for the flip-book experiment (Program 8).

Filenames should be sortable: `frame_001.jpg`, `frame_002.jpg`, ...

The program registers each frame to its predecessor and exports a
`flipbook.gif` / `flipbook.mp4` to `outputs/`.
