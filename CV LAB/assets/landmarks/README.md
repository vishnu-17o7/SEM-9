# Drop pairs of landmark photos here for the feature-detector experiment (Program 3).

Pair them as `<name>_a.jpg` and `<name>_b.jpg` (the underscore-a / -b
suffix matters). On first run, the program will try to auto-fetch a few
canonical pairs; you can also just drop your own.
