# Drop demo images here (auto-downloaded if absent).

The individual programs will fetch a small set of public-domain demo
images into this folder on first run. You can also drop your own files
in here to override the defaults.
